version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: roi
        LoadBalancerInfo:
          ContainerName: "python-app-container"
          ContainerPort: 5000
        PlatformVersion: "LATEST"